#include <cstdio>
#include <cstring>
#include <iostream>

using namespace std;

bool allDiff(string a, string b){
  
}

int main(){
  string a, b; cin >> a >> b;
  if(a == b){
    cout << 0 << endl;
  }else{
    if(allDiff(a, b)){
      cout << -1 << endl;
    }else{
      
    }
  }
  return 0;
}
