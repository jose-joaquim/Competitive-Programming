#include <iostream>
#include <cstdio>
#include <string.h>
#include <vector>

using namespace std;

typedef pair<int, int> ii;

vector<ii> aux;

int main(int argc, char **argv)
{
	int n, m;
	while(scanf("%d %d", &n, &m) && (n || m)){
		vector<ii> num_troca(n);
		for(int i = 0; i < n; i++){
			scanf("%d", &troca_num[i].second);
		}
	}
	return 0;
}

