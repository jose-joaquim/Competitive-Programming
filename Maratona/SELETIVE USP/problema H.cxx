#include <iostream>
#include <cstdio>
#include <string.h>
#include <math.h>

using namespace std;

int main(int argc, char **argv)
{
	int n, m;
	scanf("%d %d", &n, &m);
	int taylor[n], katy[m];
	for(int i = 0; i < n; i++){
		scanf("%d", &taylor[n]);
	}
	for(int i = 0; i < m; i++){
		scanf("%d", &katy[m]);
	}
	return 0;
}

