#include <string>
#include <cstdio>
#include <iostream>
#include <vector>
#include <cstring>

using namespace std;

int main() {
  ios::sync_with_stdio(0);
  int n; cin >> n;
  int a[n], b[n];
  for (int i = 0; i < n; i++) {
    cin >> a[i] >> b[i];
  }
  return 0;
}
